# Sidecar binaries directory
# Built Python binaries (e.g. via PyInstaller) should be placed here
# with the target triple suffix, e.g.:
#   motion-blur-core-aarch64-apple-darwin
#   motion-blur-core-x86_64-apple-darwin
